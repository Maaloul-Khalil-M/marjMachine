[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lenvid-cli"
version = "1.0.0"
description = "Calculate total duration of all MP4 files in a directory"
dependencies = [
    "typer>=0.9.0",
]

[project.scripts]
lenvid = "lenvid:app"